# Railway dynamic port
server.port=${SERVER_PORT:8080}

# Database (Railway MySQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://shortline.proxy.rlwy.net:34643/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:RSPYIXSJPGytGOPSbTLmTKzhGbSKREBj}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Jackson (date handling)
spring.jackson.serialization.write-dates-as-timestamps=false

# JWT
jwt.secret=${JWT_SECRET:bXlfc3VwZXJfc2VjdXJlX2p3dF9zZWNyZXRfa2V5X2Zvcl90ZWFtdGFza19hcHBfMjAyNA==}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging
logging.level.com.teamtask=INFO
